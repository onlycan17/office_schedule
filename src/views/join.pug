extends base

block content
    if errorMessage 
        span=errorMessage
    div 
        a(href="/joinAdd") 사용자등록
    ul 
        each j in userList 
            li 
                a(href=`/join/${j._id}`) #{j.name}    
