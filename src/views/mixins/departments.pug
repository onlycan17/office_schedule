mixin departments(item)
    tr
        td
            a(href=`/departmentDetail/${item._id}`) #{item.name}
        td(style="text-align:center")
            a(href=`/departmentDelete/${item._id}`) ❌ 
            